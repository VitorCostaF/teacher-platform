// Stub: implementar quando a feature de prova for construída.
// Monitorar sessão expirada durante prova e salvar respostas em localStorage.
export function useProvaOfflineBackup(_sessaoId?: string) {
  // TODO: escutar authEmitter 'session-expired', salvar respostas em
  // localStorage com chave `prova_backup_${_sessaoId}` e exibir ConfirmationModal.
}
